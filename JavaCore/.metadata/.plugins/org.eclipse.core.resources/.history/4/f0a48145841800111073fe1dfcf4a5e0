package assignment2_2;

import java.security.PublicKey;
import java.time.LocalDate;

import java.util.Scanner;

public class PaymentMain {
	
	static Scanner scanner = new Scanner(System.in);
	
	public String toGetTransactionId() {
		System.out.println("Enter the Transaction id:");
		String transactionId = scanner.next();
		return transactionId;
	}
	
	public double toGetAmount() {
		System.out.println("Enter the amount:");
		double amount = scanner.nextDouble();
		return amount;
	}
	
	public String toGetCustomerName() {
		System.out.println("Enter the amount:");
		String customerName = scanner.next();
		return customerName;
	}

	public static void main(String[] args) {
		
		do {
		System.out.println("The payment options are :\n 1. Credit card payment"
				+ "\n 2. Net banking payment"
				+ "\n 3. UPI payment");
		
		System.out.println("Enter the choice:");
		int choice = scanner.nextInt();
		
		switch(choice) {
			case 1:
				
				System.out.println("Enter the Cardnumber:");
				String cardnumber = scanner.next();
				
				System.out.println("Enter the cvv:");
				String ccv = scanner.next();
				
				System.out.println("Enter the expiry date:");
				
		}
		
		
		}while();
		

	}

}
